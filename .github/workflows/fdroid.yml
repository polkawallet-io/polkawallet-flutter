name: Fdroid test

on:
  push:
    branches: [ beta ]
  pull_request:
    branches: [ beta ]

jobs:
  setup-works:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      # Setup Environment
      - uses: actions/checkout@v2

      - name: Check fdroid setup
        run: ./scripts/fdroid_init.sh
