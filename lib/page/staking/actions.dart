import 'package:flutter/cupertino.dart';

class StakingActions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('actions'),
    );
  }
}
